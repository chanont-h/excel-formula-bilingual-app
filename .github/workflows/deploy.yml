name: Auto-Deploy Streamlit Cloud

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Notify Streamlit
        run: echo "✅ Deployment Triggered for ${{ github.ref_name }}"
